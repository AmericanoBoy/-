Когда у нас есть квадратная матрица 2x2, у нас может быть до 24 различных матриц, меняющих положения элементов.
Мы покажем некоторые из них
a  b   a  b    a  c    a  c   a  d    a  d    b  a    b  a
c  d   d  c    d  b    b  d   b  c    c  b    c  d    d  c
Возможно, вы захотите сгенерировать оставшиеся до завершения набора из 24 матриц.
Учитывая определенную матрицу чисел, которая может повторяться или нет, вычислите общее количество возможных 
матриц, которые могут быть сгенерированы, изменяя положение элементов.
Например: Случай первый
A = [[1,2,3],
     [3,4,5]]   #a 2x3 rectangle matrix with number 3 twice
генерирует набор 360 различных матриц
Случай второй
A = [[1,1,1], 
     [2,2,3], 
     [3,3,3]]
генерирует набор 1260 различных матриц.
Пример третий
A = [[1,2,3],
     [4,5,6],
     [7,8,9]]
генерирует набор 362880 различных матриц

Это ката не предназначено для применения алгоритма грубой силы, чтобы попытаться подсчитать общее количество 
ошибок.
---------------------------------------------------------------------------------------------------------------
МНОГИЕ РЕШАЮТ ПОДОБГЫЕ ЗАДАЧИ С ПОМОЩЬЮ СТОРОННИХ БИБЛИОТЕК...
Вот подобный вриант решения в моем исполнении:
from math import factorial
def count_perms(a):
    z,r=[],[]
    c=0
    for i in a:
        for j in i:
            c+=1
            if j not in z:
                z.append(j)
                r.append(1)
            else:
                r[z.index(j)]+=1
    s=1
    for i in r:
        s*=factorial(i)
    return factorial(c)//s
НО!!!! Ребята, ведь данная задача легко решается без их использования..Но ведь порою интереснее блеснуть знаниями и использовать классический вариант:
def count_perms(a):
    z,r,q=[],[],[]
    c=0
    t=1
    f=1
    for i in a:
        for j in i:
            c+=1
            t*=c
            if j not in z:
                z.append(j)
                r.append(1)
                q.append(1)
            else:
                r[z.index(j)]+=1
                f*=r[z.index(j)]
                q[z.index(j)]*=r[z.index(j)]
    return t//f 
  ПОЖАЛУЙСТА, БЕЗ ВСЯКИХ БИБЛИОТЕК И СТОРОННИХ МОДУЛЕЙ!!!!
